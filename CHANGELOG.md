# Changelog

## 2.2.0
- 开始正式模块化天剑降临技能。
- 新增 `entity/HeavenlySwordEntity`，负责巨剑实体位置、下落物理、落地与驻留生命周期。
- 新增 `model/SwordModelController`，负责 ItemDisplay 模型、缩放与水平面向释放者。
- `HeavenlySword` 改为通过实体层驱动巨剑，不再直接依赖 `SwordProjectile`。
- 保留 V2.1.20 的 `+135° Z` 剑身基准姿态与水平面向玩家逻辑。
- 保留现有蓄力、下落、冲击、分层伤害、驻留和持续伤害行为。
- Maven 版本统一为 2.2.0。
- 新增 `docs/V2.2.0.md`，记录模块边界与测试重点。

## 2.1.20
- 严格从 V2.1.14 版本的巨剑模型姿态作为基准重新修改。
- **只增加一个变化：剑身水平面向释放技能的玩家。**
- V2.1.14 的 `+135° Z` 模型旋转完全保留，不再修改剑身自身的竖直姿态与剑尖方向。
- 玩家 Pitch 不参与朝向计算，只根据巨剑与释放者的 X/Z 水平位置计算 Yaw。
- 因此无论玩家从前、后、左、右或斜向释放，变化的只有“剑面朝向玩家”，不会主动修改“剑尖向下”的模型姿态。
- 蓄力、下落、落地、驻留阶段继续使用同一套 V2.1.14 模型变换。

## 2.1.19
- 根据最新实机视频确认：2.1.18 的 `X90 + Z-135` 方案仍然没有得到“剑尖向下”的正确结果，实机画面出现横向/错误姿态。
- 本次从 V2.1.14 的核心姿态方案重新回退修正：恢复 ItemDisplay 独立 LEFT `X90°` + RIGHT `Z45°`，以保持剑身垂直并让剑尖朝下。
- 保留“面向释放者”的水平 yaw：根据巨剑位置到释放者位置的 X/Z 平面方向计算朝向，不使用玩家 Pitch。
- 无论技能从前、后、左、右或斜向释放，巨剑都只绕竖直 Y 轴改变朝向，避免再次把剑身倾斜或打横。
- 蓄力、下落、落地和驻留全过程继续使用同一套模型变换，并保留真实 ItemDisplay 与至少 4 秒驻留/持续伤害。

## 2.1.18
- 根据最新实机视频确认：2.1.17 的 `X90 + Z45 + 玩家Yaw` 组合仍会让巨剑出现横向/错误姿态，未满足要求。
- 回到 2.1.14 要解决的核心目标重新制作：巨剑必须保持**垂直、剑尖向下**。
- 使用 ItemDisplay 的独立 LEFT `X90°` 与 RIGHT `Z-135°` 变换，不再使用 2.1.17 的 Z45°方案。
- 增加并保留“面向释放者”的水平朝向：根据巨剑位置到释放者位置的水平向量计算 yaw，巨剑正面始终朝向释放技能的玩家。
- 玩家 Pitch 完全不参与姿态计算，因此玩家抬头、低头或从不同方向释放技能，都不会把剑身带歪。
- 蓄力、下落、落地和至少 4 秒驻留全过程持续保持“剑尖向下 + 面向释放者”。
- 保留落地后的真实 ItemDisplay 本体与周期性持续伤害。
