# Changelog

## 2.1.17
- 按“从 2.1.14 的姿态目标重新修正”的要求继续修改巨剑显示方向，不再沿用 2.1.16 的最终姿态方案。
- 巨剑模型改为“剑尖向下”的固定垂直姿态：ItemDisplay 使用独立的 X 轴 `90°` 左旋转与 Z 轴 `45°` 右旋转，避免把两个阶段错误合并成单一 Quaternion。
- 新增释放者朝向逻辑：巨剑的 ItemDisplay yaw 根据“巨剑位置 → 释放技能玩家”的水平向量实时计算，因此无论玩家向前、向后、向左、向右或其他方向释放，巨剑始终把剑身正面朝向玩家。
- 玩家自身的 Pitch 不再参与巨剑姿态，巨剑保持垂直；面向玩家只使用水平 yaw，避免抬头/低头再次造成剑身倾斜。
- 蓄力、下落、落地和 4 秒驻留期间持续维持同一套“剑尖向下 + 面向释放者”的姿态。
- 保留落地后的真实 ItemDisplay 本体、至少 4 秒驻留和周期性持续伤害。

## 2.1.16
- 根据最新实机截图确认：2.1.15 的单一 `-45° Z` 方案虽然让剑身竖直，但实际显示方向仍然错误，剑尖没有稳定朝下。
- 改用 ItemDisplay 的两阶段固定变换：先绕 X 轴 `90°`，再绕 Z 轴 `-135°`，避免单一 Z 轴旋转在当前 ItemDisplay 模型坐标下出现方向反转问题。
- 保留 ItemDisplay 实体 yaw/pitch 强制为 `0/0`，彻底隔离玩家视角。
- 蓄力、释放、下落、落地和驻留全过程继续使用完全相同的固定模型姿态。
- 保留至少 4 秒实际时间驻留与持续伤害逻辑。

## 2.1.15
- 根据最新实机视频修正最终方向：2.1.14 的 +135° Z 能使剑身竖直，但实机确认剑尖仍然朝上。
- 改为绕 Z 轴再翻转 180°，固定使用 -45° Z（等价于 315°），保持剑身竖直并将剑尖翻转为朝下。
- 保留 ItemDisplay yaw/pitch 全程 0/0，玩家抬头、低头或转向不会影响巨剑姿态。
- 蓄力、释放、下落、落地及驻留阶段统一使用同一固定模型旋转。
- 保留至少 4 秒实际时间驻留与持续伤害逻辑。

## 2.1.14
- 根据实机截图继续修正巨剑姿态：2.1.13 的 +45° Z 会让原始手持剑模型接近横向显示，无法满足“剑身竖直、剑尖向下”。
- 恢复已在旧版本中验证过的 +135° Z 模型修正，同时保留 2.1.13 新增的 yaw/pitch 归零逻辑。
- 现在“模型自身旋转”和“玩家视角旋转”彻底分离：固定使用 +135° Z，ItemDisplay 实体 yaw/pitch 始终保持 0/0。
- 蓄力、下落、落地、驻留全过程统一使用同一固定姿态，避免释放过程中再次歪斜。
- 保留至少 4 秒的实际时间驻留与持续伤害逻辑。

## 2.1.13
- 修复真正导致仰视天空时剑身歪斜的根因：ItemDisplay 从玩家 Location 继承了 yaw/pitch，导致实体自身姿态随玩家镜头俯仰。
- SwordProjectile 统一清除目标 Location 的 yaw/pitch，并在生成、移动、落地和驻留时强制 ItemDisplay rotation 为 0/0。
- 修正巨剑模型固定旋转为 +45° Z，使原始斜向剑模型变为竖直、剑尖向下；移除错误的 180° X 旋转方案。
- 落地驻留改为以实际时间计时，默认至少完整保留 4 秒，不因服务器 tick 波动提前结束。
- 保留落地期间周期性持续伤害。

## 2.1.12
- 重新修正巨剑模型姿态：移除此前 135° Z 轴旋转。
- 使用固定 180° X 轴旋转，使原始剑模型保持竖直并让剑尖向下。
- 模型旋转与玩家 Pitch/Yaw 完全解耦，仰视天空时不再把视角造成的斜向姿态带入剑模型。
- 蓄力、下落、落地驻留阶段统一使用同一固定模型姿态。
- 修复 CircleCI 配置仍然校验/上传 2.1.0 JAR 的历史版本号问题，改为从 pom.xml 自动读取当前版本，避免构建产物与源码版本再次不一致。

## 2.1.11
- 修复玩家仰视天空释放技能时，玩家视角 Pitch/Yaw 变化导致剑身姿态偏斜的问题。
- 剑模型使用生命周期内固定的 Quaternion 旋转，玩家视角仅用于确定目标位置，不再参与 ItemDisplay 模型旋转。
- 蓄力阶段缩放更新与模型旋转解耦。
- 下落阶段仅更新剑的位置，保持固定模型姿态。
- 落地驻留继续保持同一个 ItemDisplay 剑本体及其固定姿态。
- 保留持续伤害与落地驻留功能。
- 延续粒子优化版本的效果调整。

## 2.1.8
- 修正巨剑落地后视觉上立即消失的问题。
- 保留同一个 ItemDisplay 作为落地后的真实剑本体，不复制、不替换模型。
- 落地后固定剑本体位置，默认保留 4 秒。
- 根据蓄力后的实际模型缩放计算落地高度，避免放置点过低导致整把剑埋入方块而看起来消失。
- ItemDisplay 设置为 Persistent，并设置为 Invulnerable，避免驻留期间被实体状态影响。
- 保持 V2.1.3/V2.1.7 已验证的 135° Z 轴模型方向。
- 插件启动日志版本号同步为 2.1.8。

## 2.1.5
- 巨剑命中后不再立即消失，默认在命中位置保留 4 秒。
- 新增巨剑保留期间的周期性持续伤害。
- 持续伤害默认每 10 ticks 触发一次，倍率为对应区域普通伤害的 0.10。
- 持续伤害不重复施加击退，避免目标被连续击飞。
- 新增 `skill.linger.seconds`、`skill.linger.damage-interval-ticks` 与 `skill.linger.damage-multiplier` 配置。
- 新增 LINGERING 技能状态，确保巨剑消散后才结束本次技能释放状态。

## 2.1.4
- 隐藏技能释放过程与冷却期间的聊天框提示，避免技能刷屏。
- 将技能释放状态与剩余冷却时间移动到物品栏上方的 ActionBar。
- 冷却时间以 0.1 秒精度实时更新。
- 增加 `skill.display.actionbar` 与 `skill.display.chat-message` 配置项，为后续显示控制预留配置。
- 保留 V2.1.3 已验证通过的剑模型方向与坠落姿态。

## 2.1.3
- 修正巨剑 ItemDisplay 模型朝向，使剑身垂直、剑尖向下。
- 调整 MT（Model Transform）旋转方案，避免模型继续出现斜向显示。
- 完成实机测试，确认 `/heavenlysword` 释放、模型姿态、坠落、命中、清理与再次释放正常。
- 增加 `docs/V2.1.3.md` 版本详细说明与测试记录。

## 2.1.1
- 修正巨剑 ItemDisplay 朝向。
- 强制保持剑尖向下的显示变换。
- 固定蓄力与坠落阶段的模型朝向。
- 修正小版本发布号，便于服务器区分 V2.1.0 与方向修正版。
