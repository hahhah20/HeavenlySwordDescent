# Changelog

## 2.2.6
- 制作持续型地面裂纹：落点向外辐射的断裂路径带分支与间隔，并在驻留期间持续补帧发光。
- 重做环形冲击能量：落地爆发改为完整闭合环，驻留期间持续生成双层完整能量环与周期扩散脉冲。
- 新增 `DebrisEffect`：从真实落地材质取样，使用多方向高速 BLOCK 粒子形成明显的碎石向外、向上飞溅。
- 强化剑身周围持续能量环：由 3 层提升为 4 层完整闭合旋转能量环，并叠加金色 Dust 光点。
- 剑身周围持续上升能量粒子与地面裂纹/冲击波现在统一在驻留阶段每 Tick 驱动。
- 保持已经确认正确的巨剑模型姿态、旋转与水平朝向逻辑不变。
- `pom.xml`、`plugin.yml` 版本统一更新为 2.2.6。

## 2.2.5
- 修复视频中落地后巨剑在约 0.2 秒内消失的问题。
- 将落地后的 ItemDisplay 生命周期与粒子特效彻底隔离：任何落地/驻留视觉模块异常都不会直接触发剑本体清理。
- `IMPACT` 阶段完成后立即进入受控 `LINGERING`，并在每 Tick 前后重新锚定巨剑落地点。
- 插地动画、驻留能量环和持续伤害分别隔离异常，保证剑本体至少保持完整的配置驻留时间。
- 保持已确认正确的巨剑模型姿态与朝向不变。
- `pom.xml`、`plugin.yml` 版本统一更新为 2.2.5。

## 2.2.4
- 修复巨剑落地后立即消失的生命周期问题。
- 落地后先锁定 ItemDisplay 的落地点，再执行落地视觉与伤害逻辑，避免落地特效链影响剑本体驻留。
- 将落地后的驻留流程收敛为明确的 `IMPACT -> LINGERING`，前 8 Tick 播放垂直插地/稳定动画，之后持续保持驻地巨剑与能量环。
- 驻留期间每 Tick 强制维持巨剑的落地位置，直到配置的驻留时间结束才执行清理。
- 保留地面裂纹、环形能量、碎石飞溅和剑身周围能量环特效。
- 不修改已经确认正确的巨剑模型姿态与水平朝向逻辑。
- `pom.xml`、`plugin.yml` 版本统一更新为 2.2.4。

## 2.2.3
- 修复巨剑落地后没有触发完整 `ImpactEffect` 的问题，重新接回落地闪光、冲击波与地面裂纹。
- 重做地面裂纹粒子表现，改为从落点向外辐射的裂纹路径，并使用实际地面材质碎屑。
- 修复碎石采样位置错误：目标点位于地表上方时改为向下采样真实地面方块，因此落地可正常产生碎石飞溅。
- 新增 `SwordLingerEffect`，巨剑驻留期间持续生成三层旋转能量环、剑身上升能量粒子与地面脉冲能量环。
- 巨剑落地后继续保留完整 `IMPACT -> EMBED -> LINGERING` 流程，驻留结束后再自动清理，不再在接触地面时直接失去视觉表现。
- 不修改已经确认正确的巨剑模型姿态与水平朝向逻辑。
- `pom.xml`、`plugin.yml` 与插件描述版本统一更新为 2.2.3。

## 2.2.2
- 修复技能进入驻留阶段后使用墙钟时间计时可能导致巨剑无法按时清理的问题。
- 驻留时间改为服务器 Tick 计时，确保达到配置的驻留时长后稳定执行清理。
- 强化异常清理：技能任务或落地实体发生异常时也会主动结束并移除巨剑，避免 ItemDisplay 被永久遗留。
