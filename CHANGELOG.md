# Changelog

## 2.2.2
- 修复技能进入驻留阶段后使用墙钟时间计时可能导致巨剑无法按时清理的问题。
- 驻留时间改为服务器 Tick 计时，确保达到配置的驻留时长后稳定执行清理。
- 强化异常清理：技能任务或落地实体发生异常时也会主动结束并移除巨剑，避免 ItemDisplay 被永久遗留。
- `pom.xml`、`plugin.yml` 与插件描述版本统一更新为 2.2.2。

## 2.2.1
- 修复冷却 ActionBar 在冷却结束后仍残留最后一次倒计时的问题。
- 冷却归零后主动清空天剑技能自己的 ActionBar，不再显示“冷却 0.0s/最后数值”。
- 只有玩家再次释放技能或处于新的冷却期间，才重新显示天剑技能 ActionBar。
- 关闭插件时同步清理天剑技能曾占用的 ActionBar。

## 2.2.0
- 完成天剑降临技能模块化重构。
- 新增 `skill/HeavenlySwordSkill`，负责目标锁定与 `TARGET_LOCK -> CHARGING -> FALLING -> IMPACT -> LINGERING -> CLEANUP` 流程。
- `skill/HeavenlySword` 改为兼容旧调用方的薄封装。
- 新增 `entity/HeavenlySwordEntity`，负责巨剑实体位置、下落物理、落地与驻留生命周期。
- 新增 `model/SwordModelController`，负责 ItemDisplay 模型、缩放与水平面向释放者。
- 新增 `effect/SwordEffectManager`，统一调度预警、天空能量、下落拖尾与落地冲击特效。
- `ImpactEffect` 改为纯视觉效果，不再直接执行伤害。
- 新增 `damage/SwordDamageHandler`，作为天剑技能统一伤害入口。
- `DamageManager` 改为底层分层伤害实现，并通过 `SkillConfig` 读取参数。
- 新增 `config/ModuleConfig` 和模块化 `SkillConfig`。
- 新增 `skills.yml`、`sword.yml`、`effects.yml`，并保留 `config.yml` 作为兼容回退。
- `WarningEffect`、`EnergyEffect`、`SwordTrail` 和冲击裂纹数量接入模块化配置。
- 移除旧 `skill/SwordProjectile.java` 实现。
- 保留 V2.1.20 的 `+135° Z` 剑身基准姿态与只使用水平 X/Z 面向玩家的逻辑。
- 保留蓄力、下落、冲击、分层伤害、击退、至少 4 秒驻留和持续伤害行为。
- Maven 与 `plugin.yml` 版本统一为 2.2.0。
- 更新 `docs/V2.2.0.md` 与 README，记录最终模块边界和实机测试重点。

## 2.1.20
- 严格从 V2.1.14 版本的巨剑模型姿态作为基准重新修改。
- **只增加一个变化：剑身水平面向释放技能的玩家。**
- V2.1.14 的 `+135° Z` 模型旋转完全保留，不再修改剑身自身的竖直姿态与剑尖方向。
- 玩家 Pitch 不参与朝向计算，只根据巨剑与释放者的 X/Z 水平位置计算 Yaw。
- 因此无论玩家从前、后、左、右或斜向释放，变化的只有“剑面朝向玩家”，不会主动修改“剑尖向下”的模型姿态。
- 蓄力、下落、落地、驻留阶段继续使用同一套 V2.1.14 模型变换。

## 2.1.19
- 根据最新实机视频确认：2.1.18 的 `X90 + Z-135` 方案仍然没有得到“剑尖向下”的正确结果，实机画面出现横向/错误姿态。
- 本次从 V2.1.14 的核心姿态方案重新回退修正：恢复 ItemDisplay 独立 LEFT `X90°` + RIGHT `Z45°`，以保持剑身垂直并让剑尖朝下。
- 保留“面向释放者”的水平 yaw：根据巨剑位置到释放者位置的水平向量计算朝向，巨剑正面始终朝向释放技能的玩家。
- 玩家 Pitch 完全不参与姿态计算，因此玩家抬头、低头或从不同方向释放技能，都不会把剑身带歪。
- 蓄力、下落、落地和驻留全过程继续使用同一套模型变换，并保留真实 ItemDisplay 与至少 4 秒驻留/持续伤害。

## 2.1.18
- 根据最新实机视频确认：2.1.17 的 `X90 + Z45 + 玩家Yaw` 组合仍会让巨剑出现横向/错误姿态，未满足要求。
- 回到 2.1.14 要解决的核心目标重新制作：巨剑必须保持**垂直、剑尖向下**。
- 使用 ItemDisplay 的独立 LEFT `X90°` 与 RIGHT `Z-135°` 变换，不再使用 2.1.17 的 Z45°方案。
- 增加并保留“面向释放者”的水平朝向：根据巨剑位置到释放者位置的水平向量计算 yaw，巨剑正面始终朝向释放技能的玩家。
- 玩家 Pitch 完全不参与姿态计算，因此玩家抬头、低头或从不同方向释放技能，都不会把剑身带歪。
- 蓄力、下落、落地和至少 4 秒驻留全过程持续保持“剑尖向下 + 面向释放者”。
- 保留落地后的真实 ItemDisplay 本体与周期性持续伤害。
